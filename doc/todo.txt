
- Fix "vx" when x can be > 10. Separate v and x + right align x. Also show column for y if more than one row with same "vx",
  only show y for these duplicated rows.
- Continue with the same position when sliding on/off is changed.
- Continue with (almost) the same position when ste board size is changed.
- Toggle "focus" between board and ranked moves, show an arrow next to selected ranked move if in focus +
  possible to changing selected move with <up> and <down> and animate.

- Adjust x+1 for "standing" piece I.
- Add support for [N]ext (default = no).
- Add support for Search depth (default = 0), key <1>..<8>.

- Add battle mode, computer against computer or computer against human.

- Show dots/position.



name = JTengstrand
version = 1.01

height = [7, 7, 2.5, 2.2, 1.8, 1.3, 1.0, 0.9, 0.7, 0.6, 0.5, 0.4, 0.3, 0.25, 0.2, 0.15, 0.1]
hollowsPerRow = [0.553, 0.6, 0.5, 0.4, 0.3, 0.2, 0.1]
areaWidth = [4.25, 2.39, 3.1, 2.21, 2.05, 1.87, 1.52, 1.34, 1.18, 1]
areaHeight = [0, .5, 1.19, 2.3, 3.1, 4.6, 5.6]
areaHeightEqualWallHeight = [0, .42, 1.05, 2.2, 3.1, 4.6
